[package]
name = "smartcard-sidecar"
version = "0.0.1"
description = "PC/SC sidecar process for the Qwen Code smart-card feature"
authors = ["Qwen Team"]
license = "Apache-2.0"
edition = "2021"
rust-version = "1.77.2"

[dependencies]
pcsc = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
